GEMINI_API_KEY=AIzaSyCOJWfbx_msN0TULrlsySl0LESqISw_GsM
VECTOR_STORE=faiss            # faiss | chroma
RAG_EMBED_MODEL=text-embedding-004
RAG_LLM_MODEL=gemini-1.5-flash
PERSIST_DIR=./storage
MAX_FILES=5
MAX_FILE_MB=10
# === Tuning retrieval/generation ===
CHUNK_SIZE=380
CHUNK_OVERLAP=50
RETRIEVE_TOP_K=10      # lấy nhiều candidate để đỡ miss
CONTEXT_K=6            # số đoạn đưa vào LLM
MMR_LAMBDA=0.5         # 0..1 (0 = ưu tiên đa dạng, 1 = ưu tiên sát query)
RERANK_ON=false        # true/false: bật reranker cross-encoder nếu đã cài
# === Embedding cache & concurrency ===
EMBED_CACHE_DB=./storage/embed_cache.sqlite
EMBED_CONCURRENCY=4        # số luồng gọi Gemini song song
EMBED_SLEEP_MS=0           # delay giữa các call (ms) nếu sợ rate limit
# === Hybrid retrieval ===
HYBRID_ON=false
HYBRID_ALPHA=0.6
